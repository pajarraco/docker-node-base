{
  "name": "docker-node-base",
  "version": "1.0.0",
  "description": "Base files for Node on Docker",
  "main": "src/index.js",
  "scripts": {
    "prestart": "npm install",
    "start": "nodemon src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ernesto La Fontaine",
  "license": "MIT",
  "dependencies": {
    "express": "^4.16.2"
  },
  "devDependencies": {
    "nodemon": "^1.14.12"
  }
}
